"use client";

// Re-export the new TestimonialsSection as the default export
// This maintains backwards compatibility with existing imports
export { TestimonialsSection as default } from "./Testimonials";

