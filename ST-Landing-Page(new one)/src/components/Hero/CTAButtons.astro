<div class="cta-buttons">
  <a href="/register" class="btn-primary">
    Get Started
    <span class="arrow">→</span>
  </a>
  <a href="#how-it-works" class="btn-secondary">
    Learn More
    <span class="arrow">→</span>
  </a>
</div>

<style>
  .cta-buttons { display: flex; flex-wrap: wrap; gap: 1rem; margin-top: 1.5rem; }
  .btn-primary, .btn-secondary {
    display: inline-flex; align-items: center; gap: 0.5rem;
    padding: 1rem 2rem; border-radius: 0.75rem;
    font-weight: 600; font-size: 1.125rem;
    text-decoration: none; transition: transform 0.2s, box-shadow 0.2s;
  }
  .btn-primary {
    background: linear-gradient(to right, #2857B8, #1e40af);
    color: #fff;
  }
  .btn-primary:hover { transform: scale(1.03); box-shadow: 0 10px 25px -5px rgba(40,87,184,0.4); }
  .btn-secondary {
    background: transparent;
    border: 2px solid var(--border, #e5e7eb);
    color: var(--foreground, #111827);
  }
  .btn-secondary:hover { border-color: #7c3aed; background: rgba(124,58,237,0.08); }
  .arrow { transition: transform 0.2s; }
  .btn-primary:hover .arrow, .btn-secondary:hover .arrow { transform: translateX(4px); }
</style>